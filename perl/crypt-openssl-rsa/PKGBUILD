targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-perl-crypt-openssl-rsa"
pkgver="0.31"
pkgrel="1"
pkgdesc="RSA encoding and decoding, using the openSSL libraries"
pkgdesclong=(
  "RSA encoding and decoding, using the openSSL libraries"
)
arch="amd64"
maintainer="Zextras <packages@zextras.com"
url="https://zextras.com"
depends=(
  "carbonio-perl"
  "carbonio-openssl"
)
section="libs"
priority="important"
sources=(
  "https://cpan.metacpan.org/authors/id/T/TO/TODDR/Crypt-OpenSSL-RSA-${pkgver}.tar.gz"
  "remove-guess.patch"
)
hashsums=(
  "SKIP"
  "6a20115c5b74d4398523a5d27fba8410975f7b2f3afe2016ada9f5a59121e902"
)

build() {
  cd "${srcdir}/Crypt-OpenSSL-RSA-${pkgver}"
  patch -Np1 -i ../remove-guess.patch
  perl -I /opt/zextras/common/lib/perl5 Makefile.PL INSTALL_BASE=/opt/zextras/common \
    INSTALLSITEMAN1DIR=/opt/zextras/common/share/man/man1 INSTALLSITEMAN3DIR=/opt/zextras/common/share/man/man3 \
    LIB=/opt/zextras/common/lib/perl5 LIBS="-L/opt/zextras/common/lib -lssl -lcrypto" INC="-I/opt/zextras/common/include"
  make
}

package() {
  cd "${srcdir}/Crypt-OpenSSL-RSA-${pkgver}"
  make install DESTDIR="${pkgdir}"
  find "${pkgdir}" -name '.packlist' -delete
  find "${pkgdir}" -name '*.pod' -delete
}
