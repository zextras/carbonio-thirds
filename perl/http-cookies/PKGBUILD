pkgname="carbonio-perl-http-cookies"
pkgver="6.10"
pkgrel="3"
pkgdesc="HTTP cookie jars"
arch=('x86_64')
maintainer="Zextras <packages@zextras.com>"
url="https://github.com/zextras"
depends__apt=(
  "carbonio-perl-http-date"
  "carbonio-perl-http-message"
  "carbonio-perl-math-bigint"
  "perl"
)
depends__yum=(
  "carbonio-perl-http-date"
  "carbonio-perl-http-message"
  "carbonio-perl-math-bigint"
  "perl-core"
  "perl"
)
provides__yum=(
  "perl(Win32)"
)
section="libs"
priority="important"
source=(
  "https://cpan.metacpan.org/authors/id/O/OA/OALDERS/HTTP-Cookies-${pkgver}.tar.gz"
)
sha256sums=('e36f36633c5ce6b5e4b876ffcf74787cc5efe0736dd7f487bdd73c14f0bd7007')

build() {
  cd "${srcdir}/HTTP-Cookies-${pkgver}"
  perl -I /opt/zextras/common/lib/perl5 Makefile.PL INSTALL_BASE=/opt/zextras/common \
    INSTALLSITEMAN1DIR=/opt/zextras/common/share/man/man1 INSTALLSITEMAN3DIR=/opt/zextras/common/share/man/man3 \
    LIB=/opt/zextras/common/lib/perl5 LIBS="-L/opt/zextras/common/lib -lssl -lcrypto" INC="-I/opt/zextras/common/include"
  make
}

package() {
  cd "${srcdir}/HTTP-Cookies-${pkgver}"
  make install DESTDIR="${pkgdir}"
  find "${pkgdir}" -name '.packlist' -delete
  find "${pkgdir}" -name '*.pod' -delete
}
