pkgname="carbonio-perl-io-compress"
pkgver="2.214" # renovate: datasource=cpan depName=IO::Compress
pkgrel="1"
pkgdesc="Low-Level Interface to compression libraries"
arch=('x86_64')
maintainer="Zextras <packages@zextras.com>"
url="https://github.com/zextras"
depends__apt=(
  "carbonio-perl-compress-raw-bzip2"
  "carbonio-perl-compress-raw-zlib"
  "perl"
)
depends__yum=(
  "carbonio-perl-compress-raw-bzip2"
  "carbonio-perl-compress-raw-zlib"
  "perl-core"
  "perl"
)
section="libs"
priority="important"
source=(
  "https://cpan.metacpan.org/authors/id/P/PM/PMQS/IO-Compress-${pkgver}.tar.gz"
)
sha256sums=('0cfbc4b4ac9b28f7cbf22f114e09c9a49ace9e0b6c3778432afa2cde55512d69')

build() {
  cd "${srcdir}/IO-Compress-${pkgver}"
  perl -I /opt/zextras/common/lib/perl5 Makefile.PL INSTALL_BASE=/opt/zextras/common \
    INSTALLSITEMAN1DIR=/opt/zextras/common/share/man/man1 INSTALLSITEMAN3DIR=/opt/zextras/common/share/man/man3 \
    LIB=/opt/zextras/common/lib/perl5 LIBS="-L/opt/zextras/common/lib -lssl -lcrypto" INC="-I/opt/zextras/common/include"
  make
}

package() {
  cd "${srcdir}/IO-Compress-${pkgver}"
  make install DESTDIR="${pkgdir}"
  find "${pkgdir}" -name '.packlist' -delete
  find "${pkgdir}" -name '*.pod' -delete
}
