pkgname="carbonio-perl-html-parser"
pkgver="3.76"
pkgrel="2"
pkgdesc="HTML parser class"
arch=('x86_64')
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends__apt=(
  "carbonio-perl-http-message"
  "carbonio-perl-uri"
  "perl"
)
depends__yum=(
  "carbonio-perl-http-message"
  "carbonio-perl-uri"
  "perl-core"
  "perl"
)
section="libs"
priority="important"
source=(
  "https://cpan.metacpan.org/authors/id/O/OA/OALDERS/HTML-Parser-${pkgver}.tar.gz"
)
sha256sums=('64d9e2eb2b420f1492da01ec0e6976363245b4be9290f03f10b7d2cb63fa2f61')

build() {
  cd "${srcdir}/HTML-Parser-${pkgver}"
  perl -I /opt/zextras/common/lib/perl5 Makefile.PL INSTALL_BASE=/opt/zextras/common \
    INSTALLSITEMAN1DIR=/opt/zextras/common/share/man/man1 INSTALLSITEMAN3DIR=/opt/zextras/common/share/man/man3 \
    LIB=/opt/zextras/common/lib/perl5 LIBS="-L/opt/zextras/common/lib -lssl -lcrypto" INC="-I/opt/zextras/common/include"
  make
}

package() {
  cd "${srcdir}/HTML-Parser-${pkgver}"
  make install DESTDIR="${pkgdir}"
  find "${pkgdir}" -name '.packlist' -delete
  find "${pkgdir}" -name '*.pod' -delete
}
