pkgname="carbonio-perl-convert-binhex"
pkgver="1.125"
pkgrel="2"
pkgdesc="Perl module to extract data from Macintosh BinHex files"
arch=('x86_64')
maintainer="Zextras <packages@zextras.com>"
url="https://github.com/zextras"
depends__apt=(
  "perl"
)
depends__yum=(
  "perl-core"
  "perl"
)
provides__yum=(
  "perl(Mac::Files)"
)
section="libs"
priority="important"
source=(
  "https://cpan.metacpan.org/authors/id/S/ST/STEPHEN/Convert-BinHex-${pkgver}.tar.gz"
)
sha256sums=('513591b4be46bd7eb91e83197721b4a045a9753a3dd2f11de82c9d3013226397')

build() {
  cd "${srcdir}/Convert-BinHex-${pkgver}"
  perl -I /opt/zextras/common/lib/perl5 Makefile.PL INSTALL_BASE=/opt/zextras/common \
    INSTALLSITEMAN1DIR=/opt/zextras/common/share/man/man1 INSTALLSITEMAN3DIR=/opt/zextras/common/share/man/man3 \
    LIB=/opt/zextras/common/lib/perl5
  make
}

package() {
  cd "${srcdir}/Convert-BinHex-${pkgver}"
  make install DESTDIR="${pkgdir}"
  find "${pkgdir}" -name '.packlist' -delete
  find "${pkgdir}" -name '*.pod' -delete
}
